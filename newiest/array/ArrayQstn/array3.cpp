#include <bits/stdc++.h>

int main(){
    int n, m;

    cin >> n >> m;

    int seq1[n], seq2[m]

    for(int &v: seq1){
        std::cin >> v;

}

    for(int &v: seq2){
        std::cin >> v;
    }

    int min = seq2[0];
    for(int v : seq2){
        if(v < min ){
            min = v;
        }
    }

}

