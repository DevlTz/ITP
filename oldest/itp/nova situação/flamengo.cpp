#include <bits/stdc++.h>

using namespace std;

int main(){ 

int a = a*a , b = b*b, c = c*c;
  
  cin >> a >> b >> c;

    if ( (a + b) = c || (b + c) = a || (c + a == b)){
        cout << "Pythagorean triple!";
    }
    else{
        cout <<"Not a Pythagorean triple!";
    }
 
}